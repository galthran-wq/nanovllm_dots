# virtualenv
.venv/

# model weights (9.5GB, fetched via huggingface_hub) -- anchored to repo root
# so it does NOT match the nanovllm_dots/models/ package dir.
/models/

# golden reference fixtures now live in tests/golden{,_mf}/ and ARE committed
# (~1.6 MB; the pinned reference baseline for the consistency tests). Regenerate
# with scripts/gen_golden.py if the model checkpoint changes.

# vendored upstream reference (clone of github.com/rednote-hilab/dots.tts)
reference/

# python
__pycache__/
*.pyc
*.egg-info/
*.whl

# misc
*.log

# claude code local state
.claude/
